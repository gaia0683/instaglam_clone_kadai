<h1>ポスト詳細画面</h1>

<p>内容: <%= @post.post %>
<p>写真: <%= image_tag @post.picture.url,style:'width:400px;height:240px' if @post.picture_url %>

<% unless @post.user_id == current_user.id %>
  <% if @favorite.present? %>
    <%= link_to 'お気に入り解除する', favorite_path(id: @favorite.id), method: :delete %>
  <% else %>
    <%= link_to 'お気に入りする', favorites_path(post_id: @post.id), method: :post %>
  <% end %>
<% end %>
